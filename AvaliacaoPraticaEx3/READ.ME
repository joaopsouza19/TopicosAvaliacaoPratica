-- EXERCICIO 3

-- Sobrecarga ocorre quando uma classe possui múltiplos métodos com o mesmo nome, mas com assinaturas diferentes. A assinatura de um método inclui seu nome e os tipos e/ou número de parâmetros que ele recebe. Durante a compilação, o compilador determina qual versão do método deve ser chamada com base nos argumentos passados.

-- Sobreposição ocorre quando uma classe derivada (subclasse) redefine um método que já está definido em sua classe base (superclasse). Isso é feito usando a palavra-chave override na classe derivada para indicar que o método está substituindo um método da classe base.

// Os prompts que utilizei para criar os códigos de sobrecarga e sobreposição:
// "Provide examples of method overloading and method overriding in C#."

